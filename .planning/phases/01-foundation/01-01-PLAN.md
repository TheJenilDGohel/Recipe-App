---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [pubspec.yaml, lib/main.dart, analysis_options.yaml, .github/workflows/main.yml, lib/src/core/, lib/src/features/]
autonomous: true
requirements: [OPS-01, OPS-02, TECH-01]

must_haves:
  truths:
    - "Flutter project is initialized and runs 'flutter analyze' without errors"
    - "Riverpod is configured and provides state to the root widget"
    - "GitHub Actions pipeline triggers on push to main and completes all steps"
    - "Release APK is automatically uploaded to GitHub Releases"
  artifacts:
    - path: "pubspec.yaml"
      provides: "Dependencies (Riverpod, cached_network_image, etc.)"
    - path: ".github/workflows/main.yml"
      provides: "CI/CD pipeline configuration"
    - path: "lib/main.dart"
      provides: "App entry point with ProviderScope"
  key_links:
    - from: ".github/workflows/main.yml"
      to: "GitHub Actions"
      via: "Workflow trigger"
    - from: "lib/main.dart"
      to: "Riverpod"
      via: "ProviderScope"
---

<objective>
Initialize the Flutter project foundation with a clean architecture structure, configure Riverpod for state management, and establish a fully automated CI/CD pipeline for quality assurance and delivery.

Purpose: Ensure a stable, scalable foundation and automated release process from day one.
Output: Initialized Flutter project, folder structure, and working GitHub Actions workflow.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@sow.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Project Initialization & Clean Architecture Scaffold</name>
  <files>pubspec.yaml, lib/main.dart, analysis_options.yaml, lib/src/core/, lib/src/features/</files>
  <action>
    1. Run `flutter create --org com.ivtex.recipeapp --project-name recipe_app --overwrite .` to initialize the project in the current directory.
    2. Update `pubspec.yaml`:
       - Add dependencies: `flutter_riverpod`, `riverpod_annotation`, `cached_network_image`, `hive`, `hive_flutter`, `path_provider`.
       - Add dev_dependencies: `riverpod_generator`, `build_runner`, `hive_generator`, `flutter_lints`, `custom_lint`, `riverpod_lint`.
    3. Run `flutter pub get`.
    4. Create the following folder structure:
       - `lib/src/core/` (constants, theme, common widgets, network)
       - `lib/src/features/recipes/data/` (datasources, repositories)
       - `lib/src/features/recipes/domain/` (models, entities)
       - `lib/src/features/recipes/presentation/` (providers, screens, widgets)
    5. Modify `lib/main.dart`:
       - Wrap `MyApp` with `ProviderScope`.
       - Clean up default counter code and replace with a simple "Hello Recipe App" screen.
    6. Update `analysis_options.yaml` to include `riverpod_lint` and ensure strict linting.
  </action>
  <verify>
    <automated>flutter analyze && flutter test</automated>
  </verify>
  <done>Flutter project initialized with dependencies and folder structure; analysis passes.</done>
</task>

<task type="auto">
  <name>Task 2: DevOps CI/CD Pipeline Setup</name>
  <files>.github/workflows/main.yml</files>
  <action>
    Create `.github/workflows/main.yml` with the following jobs:
    1. **lint_and_test**:
       - Runs on `ubuntu-latest`.
       - Steps: Checkout, Setup Flutter, Run `flutter pub get`, Run `flutter analyze`, Run `flutter test`.
    2. **build_and_release**:
       - Needs `lint_and_test`.
       - Runs on `ubuntu-latest`.
       - Steps: Checkout, Setup Flutter, Build Release APK (`flutter build apk --release`).
       - Use `softprops/action-gh-release@v2` to create a release and upload the APK.
       - Release name: `v${GITHUB_RUN_NUMBER}`.
       - Tag name: `v${GITHUB_RUN_NUMBER}`.
       - Ensure `GITHUB_TOKEN` is used for authentication.
    - Trigger on `push` to `main` branch.
  </action>
  <verify>
    <automated>ls .github/workflows/main.yml</automated>
  </verify>
  <done>CI/CD workflow file created and configured for automated analysis, testing, and APK release.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer → GitHub | Push access to main branch triggers automated builds |
| GitHub Actions → GitHub Releases | CI/CD pipeline requires permissions to create releases and upload assets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | .github/workflows/main.yml | mitigate | Use branch protection rules in GitHub to require PRs and status checks. |
| T-01-02 | Elevation of Privilege | GITHUB_TOKEN | mitigate | Grant minimal necessary permissions to the GITHUB_TOKEN in the workflow file (`contents: write`). |
| T-01-03 | Information Disclosure | pubspec.yaml / API Keys | mitigate | Ensure no API keys are hardcoded; use environment variables or secret managers if needed later (D-03). |
</threat_model>

<verification>
- Run `flutter analyze` to ensure no linting errors.
- Run `flutter test` to ensure base tests pass.
- Verify the existence of the `.github/workflows/main.yml` file.
- Check that `lib/main.dart` contains `ProviderScope`.
</verification>

<success_criteria>
- Flutter project scaffolded with Riverpod and Clean Architecture folders.
- `flutter analyze` and `flutter test` pass locally.
- GitHub Actions workflow is present and correctly configured to build and release the APK.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
