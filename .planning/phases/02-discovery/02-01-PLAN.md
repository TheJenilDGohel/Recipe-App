---
phase: 02-discovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [pubspec.yaml, lib/src/core/network/dio_client.dart, lib/src/features/recipes/domain/models/meal.dart, lib/src/features/recipes/data/datasources/recipe_remote_data_source.dart, lib/src/features/recipes/data/repositories/recipe_repository_impl.dart]
autonomous: true
requirements: [SMART-01]

must_haves:
  truths:
    - "Application can fetch recipe data from TheMealDB API"
    - "Models correctly parse the nested ingredient structure from API"
  artifacts:
    - path: "lib/src/features/recipes/domain/models/meal.dart"
      provides: "Meal data model"
    - path: "lib/src/features/recipes/data/datasources/recipe_remote_data_source.dart"
      provides: "API client for TheMealDB"
  key_links:
    - from: "lib/src/features/recipes/data/repositories/recipe_repository_impl.dart"
      to: "lib/src/features/recipes/data/datasources/recipe_remote_data_source.dart"
      via: "Dependency Injection"
---

<objective>
Establish the data layer for recipe discovery by integrating with TheMealDB API and implementing robust models and repositories.

Purpose: Provide a reliable data source for the UI to consume.
Output: Working API client, data models, and repository implementation.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@pubspec.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup networking and model generation dependencies</name>
  <files>pubspec.yaml, lib/src/core/network/dio_client.dart</files>
  <action>
    - Add `dio`, `shimmer`, `freezed_annotation`, `json_annotation` to dependencies.
    - Add `freezed`, `json_serializable`, `build_runner` to dev_dependencies.
    - Run `flutter pub get`.
    - Create a basic `DioClient` provider in `lib/src/core/network/dio_client.dart` with base URL `https://www.themealdb.com/api/json/v1/1/`.
  </action>
  <verify>
    <automated>flutter pub get && flutter analyze</automated>
  </verify>
  <done>Dependencies added and Dio client initialized.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define Recipe/Meal models</name>
  <files>lib/src/features/recipes/domain/models/meal.dart, lib/src/features/recipes/domain/models/meal.freezed.dart</files>
  <behavior>
    - Meal.fromJson should handle "strIngredient1" through "strIngredient20" and combine them into a List.
    - Meal.fromJson should handle "strMeasure1" through "strMeasure20" and pair them with ingredients.
  </behavior>
  <action>
    - Create `Meal` model using `@freezed`.
    - Implement a custom `fromJson` or factory to parse the flat ingredient list from TheMealDB into a clean `List<Ingredient>` or `List<String>`.
    - Run `flutter pub run build_runner build --delete-conflicting-outputs`.
  </action>
  <verify>
    <automated>flutter pub run build_runner build && flutter test</automated>
  </verify>
  <done>Models generated and parsing logic verified.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Remote Data Source and Repository</name>
  <files>lib/src/features/recipes/data/datasources/recipe_remote_data_source.dart, lib/src/features/recipes/data/repositories/recipe_repository_impl.dart</files>
  <action>
    - Create `RecipeRemoteDataSource` using `Dio`.
    - Implement methods: `searchMeals(String query)`, `getMealDetails(String id)`, `getRandomMeal()`.
    - Implement `RecipeRepositoryImpl` that consumes the data source.
    - Export providers for both using `riverpod_generator`.
  </action>
  <verify>
    <automated>flutter analyze</automated>
  </verify>
  <done>Data source and repository implemented and registered with Riverpod.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| TheMealDB → App | API response is untrusted input |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Tampering | Recipe Models | mitigate | Use safe JSON parsing and handle nulls/empty strings from API |
| T-02-01-02 | Information Disclosure | Dio Client | mitigate | Ensure no sensitive headers are logged in production (use Interceptors carefully) |
</threat_model>

<success_criteria>
1. `flutter pub run build_runner build` completes without errors.
2. `RecipeRepository` is available via Riverpod.
3. Models correctly handle the messy ingredient structure of TheMealDB.
</success_criteria>

<output>
After completion, create `.planning/phases/02-discovery/02-01-SUMMARY.md`
</output>
