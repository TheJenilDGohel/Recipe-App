---
phase: 02-discovery
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified: [lib/src/features/recipes/presentation/providers/search_provider.dart, lib/src/features/recipes/presentation/screens/home_screen.dart, lib/src/features/recipes/presentation/widgets/recipe_card.dart, lib/src/features/recipes/presentation/widgets/search_bar.dart]
autonomous: true
requirements: [SMART-04]

must_haves:
  truths:
    - "User can search for recipes using a search bar"
    - "API calls are debounced to avoid excessive requests"
    - "Recipe list updates dynamically as user types"
  artifacts:
    - path: "lib/src/features/recipes/presentation/screens/home_screen.dart"
      provides: "Home screen with search and list"
    - path: "lib/src/features/recipes/presentation/providers/search_provider.dart"
      provides: "Debounced search logic"
---

<objective>
Implement the primary discovery interface with debounced search functionality, allowing users to find recipes efficiently.

Purpose: Provide the core value of the app — finding recipes.
Output: Searchable home screen with dynamic recipe list.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/02-discovery/02-01-SUMMARY.md
@lib/src/features/recipes/data/repositories/recipe_repository_impl.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Debounced Search Provider</name>
  <files>lib/src/features/recipes/presentation/providers/search_provider.dart</files>
  <behavior>
    - searchRecipesProvider should wait for 500ms of inactivity before calling the repository.
    - If the query is empty, it should return a default list (e.g., random meals or empty).
    - It should handle the AsyncValue state (loading, data, error).
  </behavior>
  <action>
    - Use `riverpod_generator` to create an `AsyncNotifier` or `FutureProvider` for search results.
    - Implement debouncing using a `Timer` and `ref.onDispose`.
    - Integrate with `recipeRepositoryProvider` from Plan 01.
  </action>
  <verify>
    <automated>flutter test</automated>
  </verify>
  <done>Search provider implemented with debounce logic.</done>
</task>

<task type="auto">
  <name>Task 2: Create Home Screen and Search Bar</name>
  <files>lib/src/features/recipes/presentation/screens/home_screen.dart, lib/src/features/recipes/presentation/widgets/search_bar.dart</files>
  <action>
    - Create `HomeScreen` widget using `ConsumerWidget`.
    - Implement a `RecipeSearchBar` that updates a `searchQueryProvider`.
    - Use `ref.watch(searchRecipesProvider)` to display results.
  </action>
  <verify>
    <automated>flutter analyze</automated>
  </verify>
  <done>Home screen UI structure complete.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Recipe Card and List</name>
  <files>lib/src/features/recipes/presentation/widgets/recipe_card.dart</files>
  <action>
    - Create `RecipeCard` widget displaying meal name, category, and image.
    - Use `CachedNetworkImage` for efficient image loading and caching.
    - Add basic styling and tap placeholder (navigation to details in next plan).
  </action>
  <verify>
    <automated>flutter analyze</automated>
  </verify>
  <done>Recipe list items implemented with image caching.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Search Input → Provider | User input is untrusted |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | Denial of Service | Search Provider | mitigate | Implement debouncing (Task 1) to prevent API rate limiting/abuse |
</threat_model>

<success_criteria>
1. User types in search bar and results appear after a short delay.
2. Images are cached and don't flicker on scroll.
3. No API calls are made for every keystroke.
</success_criteria>

<output>
After completion, create `.planning/phases/02-discovery/02-02-SUMMARY.md`
</output>
