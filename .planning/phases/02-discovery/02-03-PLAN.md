---
phase: 02-discovery
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified: [lib/src/features/recipes/presentation/screens/recipe_detail_screen.dart, lib/src/features/recipes/presentation/widgets/recipe_shimmer.dart, lib/src/core/widgets/error_view.dart]
autonomous: true
requirements: [TECH-02, TECH-04]

must_haves:
  truths:
    - "User can view full recipe details (ingredients, instructions)"
    - "Shimmer loaders appear while data is being fetched"
    - "App displays clear error states when network fails or results are empty"
  artifacts:
    - path: "lib/src/features/recipes/presentation/screens/recipe_detail_screen.dart"
      provides: "Full recipe view"
    - path: "lib/src/features/recipes/presentation/widgets/recipe_shimmer.dart"
      provides: "Skeleton loading UI"
    - path: "lib/src/core/widgets/error_view.dart"
      provides: "Global error handling UI"
---

<objective>
Enhance user experience by adding detailed views, smooth loading states (Shimmers), and robust error handling.

Purpose: Improve perceived performance and provide a polished, resilient user experience.
Output: Detail screen, shimmer loaders, and error UI components.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/02-discovery/02-02-SUMMARY.md
@lib/src/features/recipes/domain/models/meal.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Recipe Detail Screen</name>
  <files>lib/src/features/recipes/presentation/screens/recipe_detail_screen.dart</files>
  <action>
    - Create `RecipeDetailScreen` accepting a `mealId`.
    - Fetch meal details using a new `mealDetailsProvider(id)`.
    - Display large meal image, category, area (cuisine), ingredients list, and cooking instructions.
    - Wire up navigation from `RecipeCard` (Home) to `RecipeDetailScreen`.
  </action>
  <verify>
    <automated>flutter analyze</automated>
  </verify>
  <done>Recipe detail screen functional with real data.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Shimmer/Skeleton Loaders</name>
  <files>lib/src/features/recipes/presentation/widgets/recipe_shimmer.dart, lib/src/features/recipes/presentation/screens/home_screen.dart, lib/src/features/recipes/presentation/screens/recipe_detail_screen.dart</files>
  <action>
    - Create `RecipeListShimmer` and `RecipeDetailShimmer` using the `shimmer` package.
    - Update `HomeScreen` and `RecipeDetailScreen` to show these shimmers during the `.when(loading: ...)` state of Riverpod providers.
  </action>
  <verify>
    <automated>flutter analyze</automated>
  </verify>
  <done>Shimmer loaders integrated into Home and Detail screens.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Global Error and Empty States</name>
  <files>lib/src/core/widgets/error_view.dart, lib/src/core/widgets/empty_state.dart</files>
  <action>
    - Create a reusable `ErrorView` with "Retry" button.
    - Create an `EmptyState` widget for "No recipes found".
    - Update providers or UI to display `ErrorView` during `.when(error: ...)` state.
    - Implement a simple Snackbar notification helper for transient errors.
  </action>
  <verify>
    <automated>flutter analyze</automated>
  </verify>
  <done>Global error and empty states implemented and wired up.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| UI → User | Visual feedback must be accurate |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Information Disclosure | Error UI | mitigate | Ensure technical error details (stack traces) are not shown to the end user in `ErrorView` |
</threat_model>

<success_criteria>
1. User sees a skeleton screen while waiting for recipes to load.
2. Tapping a recipe opens a detailed view with all necessary info.
3. Turning off internet displays a clear "No Internet" or Error view with a retry option.
</success_criteria>

<output>
After completion, create `.planning/phases/02-discovery/02-03-SUMMARY.md`
</output>
