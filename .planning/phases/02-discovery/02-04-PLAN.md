---
phase: 02-discovery
plan: 04
type: execute
wave: 4
depends_on: ["02-03"]
files_modified: ["lib/src/core/widgets/error_view.dart", "lib/src/features/recipes/presentation/screens/recipe_detail_screen.dart"]
autonomous: true
gap_closure: true
requirements: ["TECH-04", "SMART-01"]
must_haves:
  truths:
    - "ErrorView component compiles and displays correctly"
    - "RecipeDetailScreen compiles and displays correctly"
  artifacts:
    - path: "lib/src/core/widgets/error_view.dart"
      provides: "Global error UI"
    - path: "lib/src/features/recipes/presentation/screens/recipe_detail_screen.dart"
      provides: "Recipe detail view"
  key_links:
    - from: "lib/src/features/recipes/presentation/screens/recipe_detail_screen.dart"
      to: "lib/src/core/widgets/error_view.dart"
      via: "import"
---

<objective>
Fix compilation blockers in ErrorView and RecipeDetailScreen identified during phase verification.

Purpose: Restore application buildability and ensure core UI components function.
Output: Valid Flutter code for ErrorView and RecipeDetailScreen.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-discovery/02-VERIFICATION.md
@.planning/phases/02-discovery/02-03-SUMMARY.md

<interfaces>
From lib/src/core/widgets/error_view.dart:
```dart
class ErrorView extends StatelessWidget {
  final String message;
  final VoidCallback? onRetry;
  // ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix ErrorView syntax error</name>
  <files>lib/src/core/widgets/error_view.dart</files>
  <action>
    Locate line 46 in lib/src/core/widgets/error_view.dart.
    Add the missing closing parenthesis ')' for the Center widget (or the relevant widget wrapping the error content).
    Ensure the file passes basic syntax check.
  </action>
  <verify>
    <automated>flutter analyze lib/src/core/widgets/error_view.dart</automated>
  </verify>
  <done>ErrorView compiles without syntax errors.</done>
</task>

<task type="auto">
  <name>Task 2: Fix RecipeDetailScreen compilation errors</name>
  <files>lib/src/features/recipes/presentation/screens/recipe_detail_screen.dart</files>
  <action>
    - Change 'child' parameter to 'sliver' in the SliverPadding widget (line 54 or adjacent).
    - Correct the relative import for ErrorView to the proper path (likely 'package:recipe_app/src/core/widgets/error_view.dart' or correct relative path).
  </action>
  <verify>
    <automated>flutter analyze lib/src/features/recipes/presentation/screens/recipe_detail_screen.dart</automated>
  </verify>
  <done>RecipeDetailScreen compiles and correctly references ErrorView.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| UI -> Error State | Displaying error messages from API |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Information Disclosure | ErrorView | mitigate | Ensure ErrorView does not leak sensitive technical details (stack traces) to the user; only display user-friendly messages. |
</threat_model>

<verification>
Run `flutter analyze` on the entire project to ensure all compilation errors are resolved.
</verification>

<success_criteria>
The project builds successfully and passes static analysis.
</success_criteria>

<output>
After completion, create `.planning/phases/02-discovery/02-04-SUMMARY.md`
</output>
