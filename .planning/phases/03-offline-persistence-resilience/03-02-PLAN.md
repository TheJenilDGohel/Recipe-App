---
phase: "03-offline-persistence-resilience"
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified: ["lib/src/core/network/connectivity_provider.dart", "lib/src/core/widgets/offline_banner.dart", "lib/main.dart", "lib/src/features/recipes/presentation/providers/search_provider.dart", "lib/src/features/recipes/presentation/providers/meal_details_provider.dart"]
autonomous: true
requirements: ["OFF-03"]
must_haves:
  truths:
    - "A purple banner appears at the top when the device goes offline."
    - "The app continues to show cached recipe lists when offline instead of just failing."
    - "The banner disappears automatically when connection is restored."
  artifacts:
    - path: "lib/src/core/network/connectivity_provider.dart"
      provides: "Real-time network status monitoring"
    - path: "lib/src/core/widgets/offline_banner.dart"
      provides: "UI component for connectivity feedback"
  key_links:
    - from: "lib/main.dart"
      to: "lib/src/core/widgets/offline_banner.dart"
      via: "Widget tree integration"
---

<objective>
Implement real-time connectivity monitoring and provide visual feedback to the user via an offline banner and resilient data providers.

Purpose: To inform the user of their network status and ensure the UI reflects available (cached) data.
Output: An automatic offline banner and updated providers that handle connectivity changes.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-offline-persistence-resilience/03-CONTEXT.md
@.planning/phases/03-offline-persistence-resilience/03-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Connectivity Monitoring Provider</name>
  <files>lib/src/core/network/connectivity_provider.dart</files>
  <action>
    Implement `connectivityProvider` using `connectivity_plus`:
    - Use `StreamProvider` to expose the current connectivity status (WiFi, Mobile, None).
    - Map `ConnectivityResult` to a custom `ConnectivityStatus` enum for simplicity.
  </action>
  <verify>
    <automated>flutter analyze lib/src/core/network/connectivity_provider.dart</automated>
  </verify>
  <done>Connectivity provider correctly reflects the current network state.</done>
</task>

<task type="auto">
  <name>Task 2: Global Offline Banner Integration</name>
  <files>lib/src/core/widgets/offline_banner.dart, lib/main.dart</files>
  <action>
    1. Create `OfflineBanner` widget per UI Spec:
       - Background: `#7C3AED`.
       - Icon: `Icons.wifi_off`.
       - Text: "You are offline. Showing saved recipes."
       - Animated slide down/up behavior.
    2. Update `lib/main.dart`:
       - Wrap the `MaterialApp` home or use a `ScaffoldMessenger` / custom `Overlay` to ensure the banner is visible across all screens.
  </action>
  <verify>
    <automated>flutter test test/core/widgets/offline_banner_test.dart</automated>
  </verify>
  <done>Banner appears and disappears based on connectivity state.</done>
</task>

<task type="auto">
  <name>Task 3: Update Providers for Offline Resilience</name>
  <files>lib/src/features/recipes/presentation/providers/search_provider.dart, lib/src/features/recipes/presentation/providers/meal_details_provider.dart</files>
  <action>
    Update existing providers to be aware of connectivity:
    - `searchRecipesProvider`: If offline and query is empty, consider showing cached "last searched" or "favorites" (deferred to Plan 03). For now, ensure it doesn't just crash.
    - `mealDetailsProvider`: If offline, immediately try to load from repository (which will use cache per Plan 01).
    - Ensure errors are handled gracefully in the UI.
  </action>
  <verify>
    <automated>flutter analyze lib/src/features/recipes/presentation/providers/</automated>
  </verify>
  <done>Providers handle offline states by leveraging the repository's caching logic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| System Connectivity → App | External OS event enters the app state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Denial of Service | connectivity_provider.dart | accept | Connectivity status flutter/lag doesn't stop app functionality, just updates UI state. |
</threat_model>

<verification>
Toggle Airplane mode on emulator/device and verify:
1. Banner appears.
2. Previously viewed recipe details still load.
3. Banner disappears when network restored.
</verification>

<success_criteria>
- [ ] `ConnectivityProvider` correctly streams network changes.
- [ ] `OfflineBanner` is visually consistent with UI Spec.
- [ ] App handles transitions between online/offline without crashing.
</success_criteria>

<output>
After completion, create `.planning/phases/03-offline-persistence-resilience/03-02-SUMMARY.md`
</output>
