---
phase: 04-contextual-intelligence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [pubspec.yaml, android/app/src/main/AndroidManifest.xml, ios/Runner/Info.plist, lib/src/features/recipes/data/datasources/recipe_remote_data_source.dart, lib/src/features/recipes/domain/repositories/recipe_repository.dart, lib/src/features/recipes/data/repositories/recipe_repository_impl.dart]
autonomous: true
requirements: [SMART-03]
must_haves:
  truths:
    - "Project compiles with geolocator and geocoding dependencies"
    - "RecipeRepository has filterByArea and filterByCategory methods"
  artifacts:
    - path: "lib/src/features/recipes/domain/repositories/recipe_repository.dart"
      provides: "Expanded repository interface"
    - path: "android/app/src/main/AndroidManifest.xml"
      provides: "Location permissions"
  key_links:
    - from: "lib/src/features/recipes/data/repositories/recipe_repository_impl.dart"
      to: "lib/src/features/recipes/data/datasources/recipe_remote_data_source.dart"
      via: "method calls"
---

<objective>
Setup the foundational infrastructure for location and time-based discovery, including platform permissions and data layer extensions for area and category filtering.

Purpose: Enable the app to request location and fetch specific sub-sets of recipes from TheMealDB.
Output: Updated dependencies, platform configs, and expanded repository methods.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/src/features/recipes/data/datasources/recipe_remote_data_source.dart
@lib/src/features/recipes/domain/repositories/recipe_repository.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup dependencies and platform permissions</name>
  <files>pubspec.yaml, android/app/src/main/AndroidManifest.xml, ios/Runner/Info.plist</files>
  <action>
    - Add `geolocator: ^13.0.2` and `geocoding: ^3.0.0` to `pubspec.yaml`.
    - Run `flutter pub get`.
    - Update `android/app/src/main/AndroidManifest.xml`: Add `ACCESS_FINE_LOCATION` and `ACCESS_COARSE_LOCATION` permissions inside the `<manifest>` tag.
    - Update `ios/Runner/Info.plist`: Add `NSLocationWhenInUseUsageDescription` with a user-friendly message explaining why location is needed (e.g., "We use your location to suggest recipes popular in your region.").
  </action>
  <verify>
    <automated>flutter pub get</automated>
  </verify>
  <done>Dependencies added and platform configurations updated for location access.</done>
</task>

<task type="auto">
  <name>Task 2: Extend Data Source for filtering</name>
  <files>lib/src/features/recipes/data/datasources/recipe_remote_data_source.dart</files>
  <action>
    - Add `Future<List<Meal>> filterByArea(String area);` and `Future<List<Meal>> filterByCategory(String category);` to the `RecipeRemoteDataSource` abstract class.
    - Implement both methods in `RecipeRemoteDataSourceImpl` using `dio.get('filter.php', queryParameters: {'a': area})` and `dio.get('filter.php', queryParameters: {'c': category})`.
    - Handle null/empty responses by returning an empty list.
  </action>
  <verify>
    <automated>flutter test</automated>
  </verify>
  <done>Remote data source supports filtering by area and category.</done>
</task>

<task type="auto">
  <name>Task 3: Update Repository for filtering</name>
  <files>lib/src/features/recipes/domain/repositories/recipe_repository.dart, lib/src/features/recipes/data/repositories/recipe_repository_impl.dart</files>
  <action>
    - Add `Future<List<Meal>> filterByArea(String area);` and `Future<List<Meal>> filterByCategory(String category);` to `RecipeRepository`.
    - Implement in `RecipeRepositoryImpl`.
    - These should call the remote data source and optionally cache results using `_localDataSource.cacheMeals`.
  </action>
  <verify>
    <automated>flutter test</automated>
  </verify>
  <done>Repository provides methods for area and category-based recipe retrieval.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App → Platform | Location data request crosses here |
| App → TheMealDB | User-context based queries (Area/Category) sent to external API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Information Disclosure | Location Service | mitigate | Use 'WhenInUse' permission only, and only fetch location when discovery is active. |
| T-04-01-02 | Tampering | API Params | mitigate | Hardcode Area/Category strings from a fixed internal mapping to prevent arbitrary query injection. |
</threat_model>

<verification>
Check that `flutter pub get` passes and that the repository interfaces are correctly implemented.
</verification>

<success_criteria>
- Project compiles with new dependencies.
- `RecipeRepository` has methods for Area and Category filtering.
- Platform permission files contain location entries.
</success_criteria>

<output>
After completion, create `.planning/phases/04-contextual-intelligence/04-01-SUMMARY.md`
</output>
