---
phase: 04-contextual-intelligence
plan: 03
type: execute
wave: 3
depends_on: [04-02]
files_modified: [lib/src/features/recipes/presentation/widgets/mini_recipe_card.dart, lib/src/features/recipes/presentation/widgets/contextual_carousel.dart, lib/src/features/recipes/presentation/screens/home_screen.dart]
autonomous: false
requirements: [SMART-02, SMART-03, ENG-02]
must_haves:
  truths:
    - "Carousel shows time-appropriate heading on Home screen"
    - "Location permission is requested on first load"
  artifacts:
    - path: "lib/src/features/recipes/presentation/widgets/contextual_carousel.dart"
      provides: "Contextual discovery UI"
    - path: "lib/src/features/recipes/presentation/screens/home_screen.dart"
      provides: "Integrated discovery experience"
  key_links:
    - from: "lib/src/features/recipes/presentation/screens/home_screen.dart"
      to: "lib/src/features/recipes/presentation/widgets/contextual_carousel.dart"
      via: "widget tree"
---

<objective>
Integrate the contextual discovery logic into the user interface with a horizontal carousel on the Home screen and handle the initial location permission request flow.

Purpose: Provide users with proactive, relevant recipe suggestions in a visually appealing way.
Output: `ContextualCarousel` and `MiniRecipeCard` widgets, updated `HomeScreen`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@lib/src/features/recipes/presentation/screens/home_screen.dart
@.planning/phases/04-contextual-intelligence/04-UI-SPEC.md
<interfaces>
From lib/src/features/recipes/presentation/providers/contextual_discovery_provider.dart:
```typescript
// Provider state will be an AsyncValue<List<Meal>>
final contextualDiscoveryProvider = AutoDisposeFutureProvider<List<Meal>>(...);

// Helper for dynamic headers
final contextualDiscoveryTitleProvider = Provider<String>(...);
final contextualDiscoverySubtitleProvider = Provider<String>(...);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Carousel Widgets</name>
  <files>lib/src/features/recipes/presentation/widgets/mini_recipe_card.dart, lib/src/features/recipes/presentation/widgets/contextual_carousel.dart</files>
  <action>
    - Create `MiniRecipeCard` as per UI Spec (160px width, aspect ratio image, 12px corners).
    - Create `ContextualCarousel`:
        - Displays Title (`heading` typography) and Subtitle (`caption` typography).
        - Horizontal `ListView.builder` of `MiniRecipeCard`s.
        - Shows shimmers while `contextualDiscoveryProvider` is loading.
        - Handles empty state (hidden or fallback).
  </action>
  <verify>
    <automated>flutter test</automated>
  </verify>
  <done>Contextual carousel components are built according to UI specifications.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate Carousel into Home and handle Permissions</name>
  <files>lib/src/features/recipes/presentation/screens/home_screen.dart</files>
  <action>
    - Insert `ContextualCarousel` between `RecipeSearchBar` and the main recipe list in `HomeScreen`.
    - Only show the carousel when `searchQuery.isEmpty` (discovery mode).
    - Implement `useEffect` or `onMount` logic to check location permissions. If `denied`, prompt for permission. If `deniedForever`, fallback silently.
    - Use `ref.invalidate(contextualDiscoveryProvider)` after permission is granted to refresh suggestions.
  </action>
  <verify>
    <automated>flutter test</automated>
  </verify>
  <done>HomeScreen features contextual suggestions with seamless permission handling.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual & Functional Verification</name>
  <action>Human verification of the contextual discovery carousel and location permission flow.</action>
  <verify>Follow the steps in how-to-verify.</verify>
  <done>User confirms the UI and behavior are correct.</done>
  <what-built>Contextual Discovery Carousel and Permission Flow</what-built>
  <how-to-verify>
    1. Run the app.
    2. On Home screen, verify the "Breakfast/Lunch/Dinner" carousel appears (depending on your system time).
    3. Verify a location permission prompt appears.
    4. Grant permission: Observe the carousel subtitle update to "Popular in [Your Region]" (if mapped) and content refresh.
    5. Denial test: Deny permission and verify carousel falls back to "Trending Globally".
    6. Search test: Type in search bar and verify carousel disappears while results are shown.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → User | Permission request prompt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Information Disclosure | UI Reveal | mitigate | Ensure location error/denial states do not reveal technical details; use user-friendly "Trending Globally" fallback. |
| T-04-03-02 | Elevation of Privilege | Permissions | mitigate | Request minimum required permission (WhenInUse) only. |
</threat_model>

<verification>
Check for visual consistency with the UI Spec and smooth transitions between loading/data states.
</verification>

<success_criteria>
- Carousel appears on Home screen in discovery mode.
- Location permission is requested on first load.
- Suggestions refresh appropriately after permission grant.
- Layout remains responsive on different screen sizes.
</success_criteria>

<output>
After completion, create `.planning/phases/04-contextual-intelligence/04-03-SUMMARY.md`
</output>
