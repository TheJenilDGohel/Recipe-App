---
phase: 05-engagement-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [pubspec.yaml, android/app/src/main/AndroidManifest.xml, lib/src/core/services/notification_service.dart, lib/main.dart]
autonomous: true
requirements: [ENG-01, ENG-02]
must_haves:
  truths:
    - "Application initializes notification settings on startup"
    - "Android manifest contains required receivers and permissions for scheduling"
  artifacts:
    - path: "lib/src/core/services/notification_service.dart"
      provides: "FlutterLocalNotificationsPlugin wrapper"
    - path: "android/app/src/main/AndroidManifest.xml"
      contains: "ScheduledNotificationReceiver"
---

<objective>
Setup the foundational infrastructure for local notifications and handle initial permissions.

Purpose: Enable the app to trigger local notifications at specific times.
Output: Initialized NotificationService and configured native platforms.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dependencies and configure AndroidManifest</name>
  <files>pubspec.yaml, android/app/src/main/AndroidManifest.xml</files>
  <action>
    - Add `flutter_local_notifications` and `timezone` to `pubspec.yaml`.
    - Run `flutter pub get`.
    - In `android/app/src/main/AndroidManifest.xml`:
      - Add `android.permission.RECEIVE_BOOT_COMPLETED`, `android.permission.SCHEDULE_EXACT_ALARM`, and `android.permission.POST_NOTIFICATIONS`.
      - Add the following receivers within the `<application>` tag:
        ```xml
        <receiver android:exported="false" android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" />
        <receiver android:exported="false" android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>
        ```
  </action>
  <verify>
    <automated>grep "flutter_local_notifications" pubspec.yaml &amp;&amp; grep "ScheduledNotificationReceiver" android/app/src/main/AndroidManifest.xml</automated>
  </verify>
  <done>Dependencies added and Android project configured for notifications.</done>
</task>

<task type="auto">
  <name>Task 2: Implement NotificationService</name>
  <files>lib/src/core/services/notification_service.dart</files>
  <action>
    - Create `NotificationService` as a Riverpod provider (use `riverpod_annotation`).
    - Initialize `FlutterLocalNotificationsPlugin`.
    - Setup `InitializationSettings` for Android and iOS.
    - Initialize `timezone` data using `tz.initializeTimeZones()`.
    - Add a method `requestPermissions()` to handle permission requests for notifications (per ENG-02).
  </action>
  <verify>
    <automated>flutter analyze lib/src/core/services/notification_service.dart</automated>
  </verify>
  <done>NotificationService implemented with initialization and permission handling.</done>
</task>

<task type="auto">
  <name>Task 3: Initialize notifications in main.dart</name>
  <files>lib/main.dart</files>
  <action>
    - Modify `main()` to be `Future<void> main()`.
    - Ensure `WidgetsFlutterBinding.ensureInitialized()` is called.
    - Initialize the `NotificationService` before `runApp`.
    - Call `requestPermissions()` on the service instance.
  </action>
  <verify>
    <automated>flutter analyze lib/main.dart</automated>
  </verify>
  <done>App initializes notifications on startup.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| App -> OS Notification System | System-level triggers require permissions |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | Notification Payload | mitigate | Ensure no sensitive PII is included in notification titles/bodies |
| T-05-02 | Denial of Service | Notification Spams | mitigate | Limit scheduled notifications to 3 meal-time slots only |
</threat_model>

<success_criteria>
- App builds and runs without crashing after notification setup.
- Log indicates successful initialization of NotificationService.
</success_criteria>

<output>
After completion, create .planning/phases/05-engagement-polish/05-01-SUMMARY.md
</output>
