---
phase: 05-engagement-polish
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [lib/src/features/recipes/presentation/widgets/favorite_toggle.dart, lib/src/features/recipes/presentation/widgets/recipe_card.dart, lib/src/features/recipes/presentation/widgets/mini_recipe_card.dart, lib/src/features/recipes/presentation/screens/recipe_detail_screen.dart]
autonomous: true
requirements: [TECH-03]
must_haves:
  truths:
    - "Favorites icon animates smoothly on toggle using implicit animation"
    - "Hero transitions occur when navigating from list to detail screen"
    - "Hero tags are unique to avoid collisions"
  artifacts:
    - path: "lib/src/features/recipes/presentation/widgets/favorite_toggle.dart"
      provides: "Smooth toggle animation"
---

<objective>
Enhance the visual quality of the app through Hero transitions and smooth micro-interactions.

Purpose: Provide a premium feel and visual continuity.
Output: Animated Favorites toggle and seamless screen transitions.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Polish Favorites Toggle animation</name>
  <files>lib/src/features/recipes/presentation/widgets/favorite_toggle.dart</files>
  <action>
    - Refactor `FavoriteToggle` to use implicit animations for a "smooth" feel.
    - Use `AnimatedScale` or `TweenAnimationBuilder` to animate the icon scale when `isFav` changes.
    - Use `AnimatedSwitcher` to cross-fade between `Icons.favorite` and `Icons.favorite_border`.
    - Maintain `HapticFeedback.mediumImpact()` on tap.
  </action>
  <verify>
    <automated>flutter analyze lib/src/features/recipes/presentation/widgets/favorite_toggle.dart</automated>
  </verify>
  <done>Favorites toggle has a smooth, implicit animation.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Hero transitions in Recipe Cards</name>
  <files>lib/src/features/recipes/presentation/widgets/recipe_card.dart, lib/src/features/recipes/presentation/widgets/mini_recipe_card.dart, lib/src/features/recipes/presentation/widgets/contextual_carousel.dart</files>
  <action>
    - Wrap the recipe image in `RecipeCard` with a `Hero` widget.
    - Wrap the recipe image in `MiniRecipeCard` with a `Hero` widget.
    - Ensure the `tag` is unique by including a source prefix, e.g., `Hero(tag: 'meal-${meal.id}-home', ...)` or passing a `heroTag` parameter to the widgets.
    - Recommendation: Add `String heroTag` to both card widgets' constructors.
  </action>
  <verify>
    <automated>flutter analyze lib/src/features/recipes/presentation/widgets/recipe_card.dart lib/src/features/recipes/presentation/widgets/mini_recipe_card.dart</automated>
  </verify>
  <done>Hero widgets added to recipe cards with unique tags.</done>
</task>

<task type="auto">
  <name>Task 3: Update RecipeDetailScreen for Hero continuity</name>
  <files>lib/src/features/recipes/presentation/screens/recipe_detail_screen.dart</files>
  <action>
    - Update `RecipeDetailScreen` constructor to accept `required String heroTag`.
    - Use this `heroTag` in the `Hero` widget that wraps the `CachedNetworkImage` in the `SliverAppBar` background.
    - Ensure all navigations to `RecipeDetailScreen` (from `home_screen`, `favorites_screen`, `search`, etc.) pass the correct `heroTag`.
  </action>
  <verify>
    <automated>flutter analyze lib/src/features/recipes/presentation/screens/recipe_detail_screen.dart</automated>
  </verify>
  <done>RecipeDetailScreen updated to support dynamic Hero tags for seamless transitions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| UI -> Rendering Engine | Animations must be performant to avoid jank |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Service Interruption | Animation Jank | mitigate | Use simple implicit animations that don't trigger heavy rebuilds |
</threat_model>

<success_criteria>
- Navigation from any list/carousel to Detail screen shows a smooth image transition.
- Toggling favorites shows a pleasing heart "pop" and color transition.
</success_criteria>

<output>
After completion, create .planning/phases/05-engagement-polish/05-03-SUMMARY.md
</output>
