---
phase: "06-ui-refinement"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/main.dart
  - lib/src/features/recipes/presentation/widgets/recipe_card.dart
  - lib/src/features/recipes/presentation/widgets/mini_recipe_card.dart
  - lib/src/features/recipes/presentation/widgets/search_bar.dart
  - lib/src/features/recipes/presentation/screens/home_screen.dart
  - lib/src/features/recipes/presentation/screens/recipe_detail_screen.dart
  - lib/src/features/recipes/presentation/widgets/recipe_shimmer.dart
  - lib/src/core/widgets/empty_state.dart
autonomous: true
requirements: [TECH-03, UI-REF-01]

must_haves:
  truths:
    - "The app uses a consistent, sophisticated color palette (Muted Creams/Charcoal/Earth Tones)."
    - "Typography has clear hierarchy with distinct weights for titles vs bodies."
    - "Recipe cards have soft, modern shadows and generous border radii (16-24dp)."
    - "Home screen has improved whitespace and a minimalist search bar."
    - "Recipe Detail screen presents ingredients and instructions in a structured, premium layout."
  artifacts:
    - path: "lib/main.dart"
      provides: "Global theme definition"
    - path: "lib/src/features/recipes/presentation/widgets/recipe_card.dart"
      provides: "Modernized main recipe card"
    - path: "lib/src/features/recipes/presentation/screens/recipe_detail_screen.dart"
      provides: "Refined detail layout"
  key_links:
    - from: "lib/main.dart"
      to: "App Widgets"
      via: "ThemeData"
    - from: "lib/src/features/recipes/presentation/widgets/recipe_card.dart"
      to: "lib/src/features/recipes/presentation/screens/home_screen.dart"
      via: "Widget usage"
---

<objective>
Overhaul the UI to match the minimalist, premium aesthetic of the reference image located at .planning/7d3d54540845a7d2628f859f98324fd5.webp.

Purpose: Elevate the app from functional to production-grade polish with a modern "feel".
Output: Refined theme, modernized cards, and polished screen layouts.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ui-refinement/06-CONTEXT.md

<interfaces>
From lib/main.dart:
```dart
class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context);
}
```

From lib/src/features/recipes/presentation/widgets/recipe_card.dart:
```dart
class RecipeCard extends StatelessWidget {
  final Meal meal;
  final VoidCallback? onTap;
  final String? heroTag;
}
```

From lib/src/features/recipes/presentation/widgets/mini_recipe_card.dart:
```dart
class MiniRecipeCard extends StatelessWidget {
  final Meal meal;
  final VoidCallback onTap;
  final String? heroTag;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sophisticated Theme & Typography</name>
  <files>lib/main.dart</files>
  <action>
    Update the MaterialApp theme to use a sophisticated, minimalist palette.
    - Set `useMaterial3: true`.
    - Define a `ColorScheme` using a muted seed color (e.g., Charcoal #2D3142 or a soft Earth tone). Avoid vibrant oranges.
    - Implement a custom `TextTheme` with clear hierarchy:
      - `titleLarge`: Semi-bold/Bold, clean sans-serif.
      - `bodyMedium`: Regular, improved line height (1.5).
      - `labelSmall`: Uppercase, increased letter spacing for category chips.
    - Update `InputDecorationTheme` for a minimalist Search Bar look (rounded, subtle fillColor, thin or no borders).
    - Ensure `AppBarTheme` is clean with no elevation and centered titles.
  </action>
  <verify>
    App builds and shows new theme colors and typography across all screens.
  </verify>
  <done>ThemeData updated and applied globally.</done>
</task>

<task type="auto">
  <name>Task 2: Modern Card Designs</name>
  <files>lib/src/features/recipes/presentation/widgets/recipe_card.dart, lib/src/features/recipes/presentation/widgets/mini_recipe_card.dart</files>
  <action>
    Refactor RecipeCard and MiniRecipeCard for high-fidelity aesthetics.
    - Increase `BorderRadius` to `24.0` for all cards and image clips.
    - Replace standard `elevation` with custom `BoxShadow`: `blurRadius: 20`, `color: Colors.black.withOpacity(0.05)`, `offset: Offset(0, 10)`.
    - Improve optical alignment: 
      - Adjust padding (use 16.0 or 20.0 instead of 12.0).
      - Ensure category chips and area text are vertically centered and balanced.
    - For `RecipeCard`, use a very subtle bottom-to-top gradient on the image if text is overlaid, or ensure the container below the image has enough breathing room.
    - Add a subtle transition or hover-like effect if possible using implicit animations.
  </action>
  <verify>
    Cards in Home and Carousel show rounded corners and soft shadows.
  </verify>
  <done>Recipe cards modernized with soft aesthetics.</done>
</task>

<task type="auto">
  <name>Task 3: Refined Screen Layouts & UI Cohesion</name>
  <files>lib/src/features/recipes/presentation/screens/home_screen.dart, lib/src/features/recipes/presentation/screens/recipe_detail_screen.dart, lib/src/features/recipes/presentation/widgets/search_bar.dart, lib/src/features/recipes/presentation/widgets/recipe_shimmer.dart, lib/src/core/widgets/empty_state.dart</files>
  <action>
    Polish the major screens and supporting UI components.
    - **Home Screen**: Increase whitespace between the Search Bar and the Carousel/List. Use a consistent 24.0dp margin for main sections.
    - **Search Bar**: Ensure it matches the minimalist theme (Task 1). Make it fully rounded with a soft shadow.
    - **Recipe Detail**:
      - Redesign the Ingredients list: Instead of simple Rows, use small cards or a clean list with custom bullet points (not just Icons).
      - Redesign Instructions: Improve readability with better line height (1.6) and structured spacing between paragraphs.
      - Use a transparent AppBar that becomes solid on scroll if possible, or keep the current SliverAppBar but with a cleaner title/back button.
    - **UI Cohesion**:
      - Update `RecipeCardShimmer` and `RecipeDetailShimmer` to use the same `24.0` border radius and new theme colors.
      - Update `EmptyState` with new typography and a more subtle icon color.
  </action>
  <verify>
    - Home screen looks spacious and premium.
    - Recipe Detail screen is highly readable and visually balanced.
    - Shimmers and Empty States match the new design language.
  </verify>
  <done>Overall UI refined and aligned with modern aesthetic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI -> User | Visual feedback and data presentation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | UI Components | accept | Standard UI widgets, no sensitive data exposure in refined layouts. |
| T-06-02 | Denial of Service | Complex UI | mitigate | Use standard Flutter widgets and avoid excessive nested animations to maintain performance. |
</threat_model>

<verification>
- Run the app on an emulator/device.
- Manually verify the theme transition (colors, fonts).
- Check Search Bar, Recipe Cards, and Detail Screen against the "minimalist premium" goal.
- Verify Shimmers appear during loading and match the new shapes.
</verification>

<success_criteria>
- App visual language is unified and sophisticated.
- Card designs are modern (soft shadows, 24dp radius).
- Detail screen is clean and structured for easy reading.
</success_criteria>

<output>
After completion, create .planning/phases/06-ui-refinement/06-01-SUMMARY.md
</output>
