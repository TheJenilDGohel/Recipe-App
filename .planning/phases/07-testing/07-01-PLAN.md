---
phase: 07-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [TECH-04, QA-01]
user_setup:
  - service: OpenMob
    why: "E2E testing on physical device"
    env_vars: []
    dashboard_config:
      - task: "Connect OnePlus device"
        location: "Terminal: adb connect 192.168.0.102:40601"

must_haves:
  truths:
    - "App launches to DISCOVER screen"
    - "Contextual Carousel subtitle reflects location/fallback"
    - "Search for 'Chicken' returns results"
    - "Recipe Detail screen shows correct data"
    - "Recipe favorited in Detail appears in Favorites list"
  artifacts:
    - path: ".planning/phases/07-testing/07-VERIFICATION.md"
      provides: "Full test report with screenshots"
  key_links:
    - from: "Discover Screen"
      to: "Search Screen"
      via: "Bottom Navigation / Search Bar"
    - from: "Search Results"
      to: "Recipe Detail"
      via: "Tap on card"
    - from: "Recipe Detail"
      to: "Favorites Screen"
      via: "Favorite Toggle + Navigation"
---

<objective>
Verify the entire application flow on a physical OnePlus device (192.168.0.102:40601) using OpenMob.
This ensures all features implemented in Phases 1-6 work together correctly in a real environment.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-testing/07-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Environment Setup & Home Screen Verification</name>
  <files>N/A</files>
  <action>
    - Ensure the device 192.168.0.102:40601 is connected using list_devices.
    - Launch the app com.ivtex.recipeapp.recipe_app.
    - Verify the "DISCOVER" title is present on the home screen.
    - Find the Contextual Carousel.
    - Verify the carousel subtitle (e.g., "Popular in India", "Trending Globally") is visible.
    - Take a screenshot of the Home screen.
  </action>
  <verify>
    <automated>openmob list_devices</automated>
  </verify>
  <done>App launched and Home screen verified with dynamic subtitle.</done>
</task>

<task type="auto">
  <name>Task 2: Search & Detail Navigation Verification</name>
  <files>N/A</files>
  <action>
    - Navigate to the Search tab or tap the search bar.
    - Type "Chicken" into the search field and submit.
    - Verify that at least one recipe card is displayed in the results.
    - Tap on the first recipe card from the results.
    - Verify the Recipe Detail screen opens by checking for typical elements like "Ingredients" or "Instructions".
    - Take a screenshot of the Search results and the Detail screen.
  </action>
  <verify>
    <automated>openmob get_current_activity</automated>
  </verify>
  <done>Search results displayed and Detail screen navigation verified.</done>
</task>

<task type="auto">
  <name>Task 3: Favorites Lifecycle Verification</name>
  <files>N/A</files>
  <action>
    - While on the Recipe Detail screen, locate the Favorite heart icon.
    - Tap the Favorite icon to add the recipe to favorites.
    - Go back to the main navigation.
    - Navigate to the "Favorites" screen.
    - Verify the recipe selected in the previous step appears in the list.
    - Take a screenshot of the Favorites screen.
    - Tap the favorite icon again on the card to remove it, and verify it disappears (optional but recommended).
  </action>
  <verify>
    <automated>openmob get_ui_tree</automated>
  </verify>
  <done>Favorites flow (Add, View, Remove) verified end-to-end.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Comprehensive E2E Test Suite Execution</what-built>
  <how-to-verify>
    1. Review the generated screenshots in the summary.
    2. Confirm that all key flows (Discover, Search, Detail, Favorites) are visually correct.
    3. Ensure the location-based discovery showed the expected context.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Device -> OpenMob Hub | ADB commands and UI tree data cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | Device Connection | mitigate | Use authorized ADB connections only |
| T-07-02 | Information Disclosure | UI Tree | mitigate | Do not log PII if present in UI tests |
</threat_model>

<success_criteria>
- All 5 testing steps defined in the goal are successfully executed and verified.
- Screenshots for each key state are captured.
- No crashes observed during the entire flow.
</success_criteria>

<output>
After completion, create .planning/phases/07-testing/07-01-SUMMARY.md with the test report.
</output>
